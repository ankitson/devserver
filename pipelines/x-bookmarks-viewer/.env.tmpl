# Rendered to .env via `op inject -i .env.tmpl -o .env`.
# Reads the same pipeline_dbos database the DBOS X-bookmarks pipeline writes to.
DATABASE_URL=postgresql://garmin:{{ op://clankers/local-service/password }}@postgres:5432/pipeline_dbos
