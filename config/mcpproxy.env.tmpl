# MCPProxy administration credential. Downstream MCP clients use a separate
# scoped agent token stored in the clankers/mcpproxy-agents item.
MCPPROXY_API_KEY={{ op://clankers/mcpproxy-admin/password }}
