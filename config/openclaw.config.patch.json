{
  "mcp": {
    "servers": {
      "mcpproxy": {
        "transport": "streamable-http",
        "url": "${MCPPROXY_GATEWAY_URL}",
        "headers": {
          "Authorization": "Bearer ${MCPPROXY_AGENT_TOKEN}"
        }
      }
    }
  }
}
